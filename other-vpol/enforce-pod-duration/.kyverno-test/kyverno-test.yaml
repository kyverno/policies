apiVersion: cli.kyverno.io/v1alpha1
kind: Test
metadata:
  name: pod-lifetime
policies:
- ../enforce-pod-duration.yaml
resources:
- resources.yaml
results:
- isValidatingPolicy: true
  kind: Pod
  policy: pod-lifetime
  resources:
  - test-lifetime-fail
  result: fail
- isValidatingPolicy: true
  kind: Pod
  policy: pod-lifetime
  resources:
  - test-lifetime-pass
  result: pass

