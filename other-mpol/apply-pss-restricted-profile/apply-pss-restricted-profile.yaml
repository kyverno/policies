apiVersion: policies.kyverno.io/v1alpha1
kind: MutatingPolicy
metadata:
  name: apply-pss-restricted-profile
  annotations:
    policies.kyverno.io/title: Apply PSS Restricted Profile
    policies.kyverno.io/category: Other, PSP Migration
    kyverno.io/kyverno-version: 1.15.0
    kyverno.io/kubernetes-version: "1.23"
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      Pod Security Standards define the fields and their options which
      are allowable for Pods to achieve certain security best practices. While
      these are typically validation policies, workloads will either be accepted or
      rejected based upon what has already been defined. It is also possible to mutate
      incoming Pods to achieve the desired PSS level rather than reject. This policy
      sets all the fields necessary to pass the PSS Restricted profile. Note that it does
      not attempt to remove non-compliant volumes and volumeMounts. Additional policies
      may be employed for this purpose.
spec:
  matchConstraints:
    resourceRules:
    - apiGroups: [""]
      apiVersions: ["v1"]
      operations: ["CREATE", "UPDATE"]
      resources: ["pods"]
    - apiGroups: ["apps"]
      apiVersions: ["v1"]
      operations: ["CREATE", "UPDATE"]
      resources: ["deployments", "daemonsets", "statefulsets", "replicasets"]
    - apiGroups: ["batch"]
      apiVersions: ["v1"]
      operations: ["CREATE", "UPDATE"]
      resources: ["jobs", "cronjobs"]

  mutations:
  # ===== PODS =====
  - patchType: JSONPatch
    jsonPatch:
      expression: |
        object.kind == "Pod" ?
        [
          JSONPatch{
            op: "add",
            path: "/spec/securityContext",
            value: dyn({
              "seccompProfile": dyn({"type": dyn("RuntimeDefault")}),
              "runAsNonRoot": dyn(true),
              "runAsUser": dyn(1000),
              "runAsGroup": dyn(3000),
              "fsGroup": dyn(2000)
            })
          }
        ] +
        object.spec.containers.map(container,
          JSONPatch{
            op: "add",
            path: "/spec/containers/" + string(object.spec.containers.indexOf(container)) + "/securityContext",
            value: dyn({
              "privileged": dyn(false),
              "allowPrivilegeEscalation": dyn(false),
              "capabilities": dyn({"drop": dyn(["ALL"])})
            })
          }
        ) : []

  # ===== DEPLOYMENTS, DAEMONSETS, STATEFULSETS, REPLICASETS =====
  - patchType: JSONPatch
    jsonPatch:
      expression: |
        object.kind in ["Deployment", "DaemonSet", "StatefulSet", "ReplicaSet"] ?
        [
          JSONPatch{
            op: "add",
            path: "/spec/template/spec/securityContext",
            value: dyn({
              "seccompProfile": dyn({"type": dyn("RuntimeDefault")}),
              "runAsNonRoot": dyn(true),
              "runAsUser": dyn(1000),
              "runAsGroup": dyn(3000),
              "fsGroup": dyn(2000)
            })
          }
        ] +
        object.spec.template.spec.containers.map(container,
          JSONPatch{
            op: "add",
            path: "/spec/template/spec/containers/" + string(object.spec.template.spec.containers.indexOf(container)) + "/securityContext",
            value: dyn({
              "privileged": dyn(false),
              "allowPrivilegeEscalation": dyn(false),
              "capabilities": dyn({"drop": dyn(["ALL"])})
            })
          }
        ) : []

  # ===== JOBS =====
  - patchType: JSONPatch
    jsonPatch:
      expression: |
        object.kind == "Job" ?
        [
          JSONPatch{
            op: "add",
            path: "/spec/template/spec/securityContext",
            value: dyn({
              "seccompProfile": dyn({"type": dyn("RuntimeDefault")}),
              "runAsNonRoot": dyn(true),
              "runAsUser": dyn(1000),
              "runAsGroup": dyn(3000),
              "fsGroup": dyn(2000)
            })
          }
        ] +
        object.spec.template.spec.containers.map(container,
          JSONPatch{
            op: "add",
            path: "/spec/template/spec/containers/" + string(object.spec.template.spec.containers.indexOf(container)) + "/securityContext",
            value: dyn({
              "privileged": dyn(false),
              "allowPrivilegeEscalation": dyn(false),
              "capabilities": dyn({"drop": dyn(["ALL"])})
            })
          }
        ) : []

  # ===== CRONJOBS =====
  - patchType: JSONPatch
    jsonPatch:
      expression: |
        object.kind == "CronJob" ?
        [
          JSONPatch{
            op: "add",
            path: "/spec/jobTemplate/spec/template/spec/securityContext",
            value: dyn({
              "seccompProfile": dyn({"type": dyn("RuntimeDefault")}),
              "runAsNonRoot": dyn(true),
              "runAsUser": dyn(1000),
              "runAsGroup": dyn(3000),
              "fsGroup": dyn(2000)
            })
          }
        ] +
        object.spec.jobTemplate.spec.template.spec.containers.map(container,
          JSONPatch{
            op: "add",
            path: "/spec/jobTemplate/spec/template/spec/containers/" + string(object.spec.jobTemplate.spec.template.spec.containers.indexOf(container)) + "/securityContext",
            value: dyn({
              "privileged": dyn(false),
              "allowPrivilegeEscalation": dyn(false),
              "capabilities": dyn({"drop": dyn(["ALL"])})
            })
          }
        ) : []