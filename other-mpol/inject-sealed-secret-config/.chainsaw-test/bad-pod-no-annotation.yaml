apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-no-annotation
  namespace: default
  labels:
    coco: enabled
spec:
  containers:
  - name: app
    image: nginx:1.20
    env:
    - name: EXISTING_VAR
      value: "existing-value"