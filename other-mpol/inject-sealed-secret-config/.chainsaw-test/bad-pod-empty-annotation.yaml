apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-empty-annotation
  namespace: default
  labels:
    coco: enabled
  annotations:
    coco.io/sealed-secret-config: ""
spec:
  containers:
  - name: app
    image: nginx:1.20
    env:
    - name: EXISTING_VAR
      value: "existing-value"