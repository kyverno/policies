apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod2
  namespace: default
spec:
  containers:
  - image: harbor.example.com/k8s/nginx:latest
    name: without-registry
  initContainers:
  - command:
    - sh
    - -c
    - echo The app is running! && sleep 3600
    image: harbor.example.com/k8s/busybox:latest
    name: init-without-registry