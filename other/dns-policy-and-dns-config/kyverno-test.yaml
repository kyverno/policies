name: dns-policy
policies:
  - dns-policy-and-dns-config.yaml
resources:
  - resource.yaml
variables: variables.yaml
results:
  - policy: dns-policy
    rule: dns-policy
    resource: myapp-pod
    patchedResource: patchedResource.yaml
    kind: Pod
    namespace: default
    result: pass