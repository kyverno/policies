apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: deny-creation-of-resources
spec:
  steps:
  - name: test-deny-creation-of-resources
    try:
    - apply:
        file: ../deny-creation-of-resources.yaml
    - assert:
        file: policy-assert.yaml
    - script:
        content: |
          current_time=$(date -u +%H)
          current_time_1hour=$(date -u -v +1H +%H)
          sed -e "s/value: .*/value: ${current_time}-${current_time_1hour}/" -e  's/validationFailureAction: Audit/validationFailureAction: Enforce/' ../deny-creation-of-resources.yaml | kubectl apply -f -
    - assert:
        file: enforce-policy-assert.yaml
    - apply:
        expect:
        - check:
            ($error != null): true
        file: resource.yaml
    - script:
        content: |
          current_time_2hours=$(date -u -v +2H +%H)
          current_time_3hours=$(date -u -v +3H +%H)
          sed -e "s/value: .*/value: ${current_time_2hours}-${current_time_3hours}/" -e  's/validationFailureAction: Audit/validationFailureAction: Enforce/' ../deny-creation-of-resources.yaml | kubectl apply -f -
    - assert:
        file: enforce-policy-assert.yaml
    - apply:
        file: resource2.yaml

